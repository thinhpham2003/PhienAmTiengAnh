# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestJson' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'RxSwift'
  pod 'RxCocoa'
  
  
  # Pods for TestJson
  pod 'RealmSwift'
  target 'TestJsonTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'TestJsonUITests' do
    # Pods for testing
  end
  
end
